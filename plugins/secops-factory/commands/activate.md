---
description: "Set the SecOps Factory orchestrator as this project's default agent (the SOC companion persona)"
argument-hint: "[--dry-run]"
---

Use the `secops-factory activate` skill via the Skill tool.

Arguments: $ARGUMENTS
