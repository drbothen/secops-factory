# Hook scripts must keep LF endings — they execute on macOS/Linux/Git Bash,
# and the .ps1 siblings are kept LF for byte-identical parity testing.
*.sh text eol=lf
*.ps1 text eol=lf
*.bats text eol=lf
