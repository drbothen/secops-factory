---
description: "Remove the SecOps Factory orchestrator default-agent override (restore normal Claude persona)"
---

Use the `secops-factory deactivate` skill via the Skill tool.

Arguments: $ARGUMENTS
